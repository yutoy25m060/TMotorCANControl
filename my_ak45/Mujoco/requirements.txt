# MuJoCo 関連リポジトリ向け統合 requirements
# 複数の環境を 1 つの requirements.txt で管理するため、用途別に整理しています。

# --- Core (必須) ---
# sysid extra（= mujoco.sysid モジュール、AK45-36_sysid_作業手順.md フェーズ2〜4で使用）は
# mujoco 3.5.0 で追加された（PyPIメタデータ上、3.4.0までのprovides_extraはusdのみ）。
# 旧 3.3.4 ピンには理由の記録がなく、3.5.0台への引き上げに支障がないため sysid を含めて統合した。
# extra が absl-py/colorama/imageio[ffmpeg]/jinja2/matplotlib/plotly/pyyaml/scipy/tabulate/
# typing_extensions をまとめて入れるため、それらの明示指定は不要。
# TODO: sysidのAPIで動作確認が取れた時点で、>= ではなく実際に動いたバージョンに固定する
#       （比較的新しいAPIのためバージョン間で変わる可能性がある）
mujoco[sysid]>=3.5.0
numpy
scipy
glfw
PyOpenGL
matplotlib

# --- RL / Training ---
gymnasium
gymnasium-robotics
stable-baselines3[extra]

# --- MJX / JAX ---
mujoco-mjx
jax
jaxlib
etils[epath]
trimesh
absl-py

# --- Visualization / UI ---
PyQt6

# --- Notebook / Utility ---
pillow
opencv-python
moviepy

# --- sysid: 実機CSVログ（data/raw/）を sysid.TimeSeries に変換するアダプタ用 ---
pandas

# --- Optional: CUDA for JAX (NVIDIA GPU 利用時のみ) ---
# jax-cuda12-plugin
# jax-cuda12-pjrt

# --- Optional: MuJoCo ドキュメント生成用 ---
# Sphinx==7.4.7
# furo==2024.8.6
# sphinxcontrib-bibtex==2.6.3
# sphinxcontrib-katex==0.9.4
# sphinxcontrib-youtube==1.2.0
# sphinx-copybutton==0.5.2
# sphinx-favicon==1.0.1
# sphinx-reredirects==0.1.1
# sphinx-toolbox==3.8.2
# nbsphinx==0.9.1
# pandoc==1.1.0
# pygments==2.19.2
# jq==1.8.0
# Jinja2==3.1.6
# markupsafe==2.0.1
